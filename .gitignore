# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/

# Local backend storage (LocalFileBackend 默认数据目录)
.yanyu_kb/
.yanyu_pending_sessions/

# Eval outputs (dashboard PNGs / mock sessions)
*.png
session*.json
!eval/fixtures/regression_set.json
dashboard*.png

# Editors
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Scrape / probe intermediates
/tmp/yanyu_*

# Lark CLI runtime artifacts
.lark/
*.session

# Secrets — NEVER commit
.env
.env.*
*.key
*_api_key*
secrets/

# Eval agreement test outputs(可能含 LLM 真调结果, 不归档)
eval/fixtures/bloom_agreement_result_*.json

# CodeGraph MCP 索引(本地缓存, 不入 git)
.codegraph/
